// Adds some convenience methods to plv8, eg plv8._executeScalar. Only needs
// to be required once.
require('../plv8-fill');

var example = require('./example');
var util = require('./util');

module.exports = {
  example: example
};

